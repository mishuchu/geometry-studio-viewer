{
  "name": "geometry-studio-viewer",
  "private": true,
  "version": "1.0.0",
  "description": "A universal, high-performance web-based viewer for geometry debugging and visualization.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext js --no-error-on-unmatched-pattern",
    "test": "node tests/run.js"
  },
  "dependencies": {
    "three": "^0.160.0",
    "tweakpane": "^4.0.1"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "eslint": "^8.57.0"
  }
}
